# Local Terraform state + lock files. Phase 7 will move to S3 + DynamoDB remote.
*.tfstate
*.tfstate.backup
.terraform/
.terraform.lock.hcl

# Real values. Committed template is terraform.tfvars.example.
terraform.tfvars
